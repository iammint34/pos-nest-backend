// POS Local Backend - Prisma Configuration (SQLite)
// Stores synced data from Portal and local orders

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
